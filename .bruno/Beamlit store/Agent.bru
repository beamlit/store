meta {
  name: Agent
  type: http
  seq: 4
}

post {
  url: http://localhost:1338?all_responses=true
  body: json
  auth: none
}

params:query {
  all_responses: true
}

body:json {
  {
    "input": "Can you retrieve pull request from my github repository, after retrieving it generate a small changelog about each one ?"
  }
}
