meta {
  name: Retrieve KPI
  type: http
  seq: 3
}

post {
  url: http://localhost:1338
  body: json
  auth: none
}

params:query {
  ~debug: true
}

body:json {
  {
    "inputs": "How can I improve my sales in countries where I am a bit behind ?"
  }
}
